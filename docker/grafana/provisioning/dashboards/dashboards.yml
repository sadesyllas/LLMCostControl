apiVersion: 1

providers:
  - name: LLM Cost Tracker
    orgId: 1
    folder: LLM Cost Tracker
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
